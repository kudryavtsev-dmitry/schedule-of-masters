import { FormikProps } from 'formik';
import { FromikRegProps } from '../../containers/RegistrationContainer/RegistrationContainer';

export interface RegistrationProps {
  formik: FormikProps<FromikRegProps>;
}
